name: first workflow
on:
  workflow_dispatch

jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: print greeting
        run: echo "hello"
      - name: print bye
        run: echo "done bye"
